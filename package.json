{
  "name": "@koax/compose",
  "description": "Compose koax middleware.",
  "repository": "git://github.com/koaxjs/compose.git",
  "version": "0.3.3",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "prepublish": "make build",
    "postpublish": "make clean",
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {
    "@f/identity": "^1.1.1",
    "@f/is-function": "^1.1.1",
    "@f/is-generator-object": "^1.3.0",
    "@f/to-generator": "^1.0.1"
  },
  "devDependencies": {
    "babel-cli": "^6.4.0",
    "babel-preset-es2015": "^6.3.13",
    "babel-register": "^6.3.13",
    "babel-polyfill": "^6.3.13",
    "tape": "^4.2.2"
  }
}
